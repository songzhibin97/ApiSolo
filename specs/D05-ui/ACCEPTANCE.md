
## R4（窄口径复核）与冻结

**R4 判决：REVISE(1C + 1I + 0M)**。本轮只查三件事，不重审全篇。

**CRITICAL — 两条 VERIFIED 都不在生产代码上**。`§11` 实际跑的是 `/tmp/urlbar-rev3.mjs` 里重新实现的协调器；`§12(a)` 的"变异"是 `const WITH_REVISION = process.env.MUT !== "1"`，切换的是**临时测试自己**的 watch 数组，而 `UrlBar.vue` 里那个 watcher 尚不存在。**处置：rev6 降为 VERIFIED 0 / DESIGNED 57 / 冗余 1。**

**IMPORTANT — 检查点 h 的观测手段行不通且有泄密风险**。理由成立（只看地址栏无法区分"刻意隐藏"与"根本没发"），但 `[network]` 行在**发送前**由 `requestSnapshot.url` 构造，而 query API key 是**进 Rust 之后**才追加的，控制台看不到；且让它看得见等于把真实密钥写进日志。**处置：改受控本地 echo endpoint + 双断言，删除 `[network]` 路径。**

**引用扫描：通过**。独立扫描 209 处 `§N` 引用，越界 0，无残留旧编号。

### VERIFIED 桶的四次修正

```
rev3 之前   51 条「独立 killer」
rev4         6
rev5         2
rev6         0   ← 唯一可能的诚实答案
```

四次修正每一次都是对的，但**终值从第一轮起就可推导**：greenfield ⇒ 生产代码不存在 ⇒ 无处打补丁 ⇒ VERIFIED 必为 0。已写成仓库规则 **P13**（main `390209b`），起因同时包含本切片与 D04。

**起草者自己交出的、切自身的观察**（值得单列）：

> 唯一两次真正碰到生产代码的实验（`request.ts`、`console.ts`），结论都是**「单塌不成立」**。真实的生产实验产出的是**证伪**，不是验证。每一个非零数字都来自别处。

以及关于检查点 h：**「一个正确的理由不等于一个安全的理由——正是它让我停止检查手段。」**

### owner 机械核对（**非独立**，仅核对 R4 两项的落实）

| 核对项 | 结果 |
|---|---|
| 三态 | `VERIFIED 0 / DESIGNED 57 / 冗余 1`（TECH:648–650）✅ |
| 检查点 h | echo endpoint + 双断言（:621–623）；`[network]` 已删并写明两个理由（:624）✅ |
| 范围 | 仅 `TECH.md`，+43/−31，未顺手改他处 ✅ |

**过程记录**：owner 首次核对用 `awk` 区间取计数，`echo` 与 `[network]` 双双返回 0。**`echo` 为 0 说明区间根本没匹配上**，该结果不构成"h 已修好"的证据。按 P12 重新定位后才取得上表结论。**度量装置自身失效时，两个 0 里有一个是假的。**

### 起草者对 owner 依据的更正

owner 与评审者均称 `src/utils/url-params.ts` 不存在。**实际存在**（D01 建立），只是不含 `reconcileUrlBarValue` / `splitTemplateSpans` / `encodeFormComponentPreservingTemplates`。结论不变，但陈述已改到**函数级**，使转正步骤指名道姓地说明要打补丁的对象。

### 冻结

**冻结版本 `rev6`**，HEAD `02bd8ce`，58 条 Behavior 不变式，映射表 58 行对齐。判据写死为三条：①转红 ②共用生产代码的邻居保持绿 ③**被变异的是落盘生产代码、被观察的是落盘测试**。①②缺③ ⇒ 仍记 DESIGNED。

冻结后任何改动必须是标注理由的受控修订。

## 阶段 4 — 实现（按 A22 拆两个 PR）

| PR | 不变式 | 依赖 |
|---|---|---|
| **PR-A** URL 栏组 | §1–§13 + A22 的 READMEs/AuthEditor | 仅 4.4-a。**无 D06 交接依赖** |
| **PR-B** 告警呈现组 | §14–§58 | 承载 D06 的 B1/B2/B3；组件层受 4.3 自检门禁 |

**PR-A 的 13 条里有 3 条是组件类**（§4、§6、§12b），harness 作废时降级为已登记验证缺口 + §4.5 的人工检查点 h/i/j，**不得计入覆盖**。三条全是**接线**断言（P8 那一类）。

**§56 跨两个 PR**：矩阵机制随 PR-A 落地（只覆盖 PR-A 新增的那个 key），扩到 20 个 key 属 PR-B，故其"已证明"状态**日期属于 PR-B**。
