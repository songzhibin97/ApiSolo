# D02 — 验收记录

本文件是「什么通过了」的唯一权威。轮次**只追加不改写**。

- 切片：D02 HTTP 报文正确性
- 分支：`songzhibin/d02-http-wire`
- 起点：`c2f0f84`（main）
- 基线（owner 亲跑）：vitest **15 files / 84 passed**；cargo **68 passed**

## 角色

| 角色 | 承担者 | 产出 |
|---|---|---|
| owner | 主会话 | 裁定、gate 亲跑、合并 |
| implementer | subagent | 代码 + 测试 + 自检报告（自检**不是**验收证据） |
| reviewer | codex CLI（独立模型） | 逐轮 verdict |

## 阶段 3 — spec 评审轮次

| 轮次 | verdict | 状态 |
|---|---|---|
| R1 | **REVISE(1C + 6I + 1M)** | 已处置 → rev2 |
| R2 | **REVISE(4C + 3I + 1M)** | 已处置 → rev3 |
| R3 | **REVISE(2C + 1I)** | 已处置 → rev4 |
| R4 | **REVISE(2C + 1I + 1M)** | 已处置 → rev5 |
| R5 | **REVISE(1C + 3I + 3M)** | 已处置 → rev6 |
| R6 | **APPROVE（0C）** | 已处置 → rev7 文本修正 |
| owner 机械核对（非独立） | 五处文本修正全部通过 | ✅ **已冻结** |

### 各轮 CRITICAL 摘要

| 轮 | 摘要 |
|---|---|
| R1 | 探测预算未覆盖 DNS，`to_socket_addrs` 无界执行 |
| R2 | 零预算 tokio 测试结论相反（实测 1000/1000 返回 Ok）；总预算仍可能 35 秒且 `total` 漏掉黑洞地址等待；identity 路径的契约自相矛盾；文件范围违反边界且漏列 `Cargo.lock` |
| R3 | deadline 未覆盖全部实际工作（build/归一化/`bytes.to_vec()` 都在窗口外）；§43/§44 只证明 helper 自身，删掉生产代码里的检查后测试仍绿 |
| R4 | 同上两条的残留形态 |
| R5 | `KWIRE3` 只证明模型接线不证明生产接线 |
| R6 | — （APPROVE） |

### 关键裁定

| # | 内容 |
|---|---|
| A11 | 批准范围例外：可改 `Cargo.toml` 与 `Cargo.lock`，**仅限依赖声明**，明确排除 `[features]` / `[lib]` / `[[example]]` / profile 段。owner 原先把边界写成「只有 `lib.rs` 的 HTTP 段」是漏考虑——加 crate 不可能不动这两个文件，而 `Cargo.lock` 漏提交会破坏可复现的离线构建 |
| — | 解压方案：**拒绝**启用 reqwest 的解压 feature，因为 tower-http 会在 `Accept-Encoding` 缺失时**注入**该头，等于在一个以字节保真为目标的切片里改掉今天正常工作的请求的报文。改手动解码，`flate2`/`brotli-decompressor` 已在锁文件中，仅新增 `encoding_rs`（+12 行锁文件，0 新增传递依赖） |
| — | 压缩炸弹：解压引入了今天不存在的 OOM 面，**64 MiB 硬上限纳入本切片**（谁制造放大谁负责）；identity 路径的上限属继承债，推给后续切片 |
| — | `KWIRE3` **诚实降级**：撤销自动化证明、登记为验证缺口，改用结构性缓解（把 build→归一化→读预算→设 timeout 封进具名函数，使人工检查点从「扫 200 行 async fn」变成「读四条语句」） |

### 本切片最有价值的三条产出——全部来自「执行推翻推理」

1. **`KFAIR` 与 `KFIRST` 的杀伤集合完全相同**，故 §37/§38 并不正交：不分配预算就会饿死第二个地址，「尝试了每个地址」也一起失败。rev4 的「独立 killer」声明因此撤回。
2. **`KDEFAULT_1252` 同时杀死三条不变式**——PNG 的 magic bytes 在 windows-1252 下是合法文本，二进制探测因此翻转。
3. **mutant 忠实性规则**（§4.7）：killer 的 `cfg` 分支必须是所描述的那**一个**改动的最小实现，两个改动就要两个 mutant。起因是 `KBUDGET` 捆绑了两处改动，导致记录的杀伤集合虚高一倍（4 条 vs 真实 2 条）。

> **该规则诞生的同一轮里，起草者又提交了一个未经核实的 mutant 行为断言**（声称旧的不忠实 mutant 在忠实基线上存活；评审重跑为 4/4 红），已删除。**规则不会自动应用到「为论证这条规则而顺手加的那句话」上——规则会豁免自己。** 同日在另一切片出现同构形态（为满足「断言须承重」而新增的断言本身是假绿），以及 owner 自己（写「禁止跨文档标识符」的规则时，规则编号与评审轮次编号相撞）。

### 冻结

**冻结版本 `rev7`**（rev6 + 五处文本修正），49 条 Behavior 不变式，映射表 50 行（§34 拆 a/b），六项验证缺口已登记。冻结后任何改动必须是标注理由的受控修订。

## 阶段 4 — 实现

**状态**：**阻塞**。D02 与 D01 都改 `src-tauri/src/lib.rs`（前者 HTTP 执行段、后者 history 段），按 backlog 的串行约束，**D02 实现必须等 D01 合并之后开始**。
